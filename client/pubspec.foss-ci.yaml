name: pocketcoder_foss_ci
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

# Minimal CI workspace. Keep this in sync with the targets checked by
# foss-purity.yml.
workspace:
  - packages/pocketcoder_api
  - packages/pocketcoder_flutter
  - apps/pocketcoder_foss
