name: foss
resolution: workspace
description: "FOSS implementations and third-party integrations for PocketCoder."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  pocketcoder_flutter:
    path: ../pocketcoder_flutter
  equatable: ^2.0.5
  # Add other FOSS-friendly dependencies for ntfy here
