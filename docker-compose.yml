services:
  # 🏰 POCKETBASE CORE (Backend Only)
  pocketbase:
    build:
      context: .
      dockerfile: docker/backend.Dockerfile
    container_name: pocketcoder-pocketbase
    ports:
      - "8090:8090" # PocketBase UI
    volumes:
      - pb_data:/app/pb_data
      # - pb_backups:/app/pb_backups
      - pocketcoder-logs:/app/pb_public/logs
      - opencode_workspace:/workspace
      - ./opencode:/workspace/.opencode
      - ./.ssh_keys:/ssh_keys
      - ./mcp_config_dir:/mcp_config
    environment:
      - POCKETBASE_SUPERUSER_EMAIL=${POCKETBASE_SUPERUSER_EMAIL}
      - POCKETBASE_SUPERUSER_PASSWORD=${POCKETBASE_SUPERUSER_PASSWORD}
      - POCKETBASE_ADMIN_EMAIL=${POCKETBASE_ADMIN_EMAIL}
      - POCKETBASE_ADMIN_PASSWORD=${POCKETBASE_ADMIN_PASSWORD}
      - AGENT_EMAIL=${AGENT_EMAIL}
      - AGENT_PASSWORD=${AGENT_PASSWORD}
      - OPENCODE_URL=${OPENCODE_URL}
      - DOCKER_HOST=tcp://docker-socket-proxy-write:2375
    command: >
      sh -c "echo '⏳ Waiting for OpenCode to be ready...' && while ! curl -I -s --connect-timeout 2 http://opencode:3000 > /dev/null; do echo 'OpenCode not yet ready, retrying in 1 second...' && sleep 1; done; echo '🚀 OpenCode is ready. Starting PocketBase...' && /app/pocketbase serve --http=0.0.0.0:8090"
    restart: unless-stopped
    networks:
      - pocketcoder-memory
      - pocketcoder-mcp

  # 🧠 OPENCODE (Reasoning Engine)
  opencode:
    build:
      context: .
      dockerfile: docker/opencode.Dockerfile
    container_name: pocketcoder-opencode
    depends_on:
      sandbox:
        condition: service_healthy
    environment:
      - PORT=3000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GOOGLE_GENERATIVE_AI_API_KEY=${GEMINI_API_KEY}
      - PROXY_URL=http://sandbox:3001
      - SHELL=/usr/local/bin/pocketcoder-shell
      - AGENT_EMAIL=${AGENT_EMAIL}
      - AGENT_PASSWORD=${AGENT_PASSWORD}
      - POCKETBASE_URL=http://pocketbase:8090
    working_dir: /workspace
    user: root
    volumes:
      - ./.ssh_keys:/ssh_keys:ro
      - opencode_workspace:/workspace
      - ./opencode:/workspace/.opencode
      - opencode_data:/root/.local/share/opencode
      - ./opencode.json:/root/.config/opencode/opencode.json:ro
      - ./mcp_config_dir:/mcp_config:ro
    command: serve --port 3000 --hostname 0.0.0.0 --log-level DEBUG
    healthcheck:
      test: ["CMD", "/bin/ash", "-c", "curl -f http://localhost:3000/health && nc -z localhost 2222"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped
    networks:
      - pocketcoder-memory
      - pocketcoder-control

  # 🔨 SANDBOX (Isolated Execution Environment)
  sandbox:
    build:
      context: .
      dockerfile: docker/sandbox.Dockerfile
    container_name: pocketcoder-sandbox
    volumes:
      - opencode_workspace:/workspace
      - ./opencode:/workspace/.opencode
      - ./agents/subagents:/root/.aws/cli-agent-orchestrator/agent-store
      - cao_db:/root/.aws/cli-agent-orchestrator/db  # CAO SQLite database
      - ./.ssh_keys:/ssh_keys:ro
      - ./sandbox/cao/src:/app/cao/src
      - ./sandbox/opencode.json:/root/.config/opencode/opencode.json:ro
    working_dir: /workspace
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GOOGLE_GENERATIVE_AI_API_KEY=${GEMINI_API_KEY}
      - TMUX_TMPDIR=/tmp/tmux
      - TMUX_SOCKET=/tmp/tmux/pocketcoder
      - SSH_KEYS_FILE=/ssh_keys/authorized_keys
      - MCP_HOST=http://mcp-gateway:8811/sse
      - DOCKER_MCP_IN_CONTAINER=1
    healthcheck:
      test: ["CMD", "/bin/bash", "-c", "curl -f http://localhost:3001/health && curl -f http://localhost:9889/health"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - pocketcoder-control
      - pocketcoder-mcp

  # 🔌 MCP GATEWAY (MCP Server Gateway)
  mcp-gateway:
    build:
      context: .
      dockerfile: docker/mcp-gateway.Dockerfile
    container_name: pocketcoder-mcp-gateway
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./mcp_config_dir:/root/.docker/mcp
    environment:
      - DOCKER_MCP_IN_CONTAINER=1
      - LINODE_TOKEN=${LINODE_TOKEN}
      - ENABLE_TF_OPERATIONS=${ENABLE_TF_OPERATIONS}
      - TFE_TOKEN=${TFE_TOKEN}
      - DOCKER_MCP_TELEMETRY_DEBUG=1
    command: ["--port", "8811", "--transport", "sse", "--verbose", "--log-calls", "--log", "/var/log/mcp-gateway.log", "--catalog", "/root/.docker/mcp/docker-mcp.yaml", "--enable-all-servers"]
    networks:
      - pocketcoder-mcp
    restart: unless-stopped

  # 🔒 DOCKER SOCKET PROXY - WRITE (Security Layer for PocketBase)
  # Currently shared by both PocketBase (restart) and Dozzle (logs)
  # For production: uncomment docker-socket-proxy-read below and update Dozzle config
  docker-socket-proxy-write:
    image: tecnativa/docker-socket-proxy:latest
    container_name: pocketcoder-docker-proxy-write
    environment:
      # Enable container operations for PocketBase restart capability
      - CONTAINERS=1          # Allow container operations (list, inspect, restart)
      - POST=1                # Allow POST requests (for restart)
      - EVENTS=1              # Allow streaming events (for Dozzle)
      - INFO=0                # Disable info endpoint
      - IMAGES=0              # Disable image operations
      - NETWORKS=0            # Disable network operations
      - VOLUMES=0             # Disable volume operations
      - BUILD=0               # Disable build operations
      - COMMIT=0              # Disable commit operations
      - CONFIGS=0             # Disable config operations
      - DISTRIBUTION=0        # Disable distribution operations
      - EXEC=0                # Disable exec operations
      - PLUGINS=0             # Disable plugin operations
      - SECRETS=0             # Disable secrets operations
      - SERVICES=0            # Disable services operations
      - SESSION=0             # Disable session operations
      - SWARM=0               # Disable swarm operations
      - SYSTEM=0              # Disable system operations
      - TASKS=0               # Disable tasks operations
      - LOG_LEVEL=info        # Logging level
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - pocketcoder-mcp
    restart: unless-stopped

  # 🔒 DOCKER SOCKET PROXY - READ (Future: Read-only for Dozzle)
  # Uncomment to separate read-only (Dozzle) from write (PocketBase) permissions
  # docker-socket-proxy-read:
  #   image: tecnativa/docker-socket-proxy:latest
  #   container_name: pocketcoder-docker-proxy-read
  #   environment:
  #     # Read-only container operations for Dozzle log viewing
  #     - CONTAINERS=1          # Allow container operations (list, inspect, logs)
  #     - POST=0                # Disable POST requests (no restart/stop/start)
  #     - EVENTS=1              # Allow streaming events (for Dozzle)
  #     - INFO=0                # Disable info endpoint
  #     - IMAGES=0              # Disable image operations
  #     - NETWORKS=0            # Disable network operations
  #     - VOLUMES=0             # Disable volume operations
  #     - BUILD=0               # Disable build operations
  #     - COMMIT=0              # Disable commit operations
  #     - CONFIGS=0             # Disable config operations
  #     - DISTRIBUTION=0        # Disable distribution operations
  #     - EXEC=0                # Disable exec operations
  #     - PLUGINS=0             # Disable plugin operations
  #     - SECRETS=0             # Disable secrets operations
  #     - SERVICES=0            # Disable services operations
  #     - SESSION=0             # Disable session operations
  #     - SWARM=0               # Disable swarm operations
  #     - SYSTEM=0              # Disable system operations
  #     - TASKS=0               # Disable tasks operations
  #     - LOG_LEVEL=info        # Logging level
  #   volumes:
  #     - /var/run/docker.sock:/var/run/docker.sock:ro
  #   networks:
  #     - pocketcoder-mcp
  #   restart: unless-stopped
  #   # When enabled, update Dozzle to use: DOCKER_HOST=tcp://docker-socket-proxy-read:2375

  # 📊 DOZZLE (Docker Log Viewer)
  dozzle:
    image: amir20/dozzle:latest
    container_name: pocketcoder-dozzle
    environment:
      - DOZZLE_LEVEL=info
      - DOZZLE_TAILSIZE=300
      - DOZZLE_FILTER=status=running
      - DOCKER_HOST=tcp://docker-socket-proxy-write:2375
    networks:
      - pocketcoder-memory    # For PocketBase reverse proxy access
      - pocketcoder-mcp       # For docker-socket-proxy access
    restart: unless-stopped
    depends_on:
      - docker-socket-proxy-write

  # 📈 SQLPAGE (SQLite Dashboard)
  sqlpage:
    image: lovasoa/sqlpage:latest
    user: root
    container_name: pocketcoder-sqlpage
    ports:
      - "8088:8080"
    environment:
      - SQLPAGE_ENVIRONMENT=development
    volumes:
      - pb_data:/database/pocketbase           # PocketBase DB (operational)
      - opencode_data:/database/opencode       # OpenCode DB (analytics)
      - cao_db:/database/cao                   # CAO DB (subagent tasks)
      - ./dashboard:/var/www:ro                  # SQLPage queries/dashboards
      - ./dashboard/config:/etc/sqlpage:ro       # Configuration (sqlpage.json, on_connect.sql)
    networks:
      - pocketcoder-memory    # For PocketBase reverse proxy access
    restart: unless-stopped
    depends_on:
      - pocketbase
      - opencode
      - sandbox

volumes:
  pb_data:
  # pb_backups:
  #   external: true
  #   name: pocketcoder_pb_backups
  pocketcoder-logs:
  opencode_workspace:
  opencode_data:
  cao_db:  # CAO SQLite database for subagent tasks

networks:
  pocketcoder-memory:
    driver: bridge
  pocketcoder-control:
    driver: bridge
  pocketcoder-mcp:
    driver: bridge