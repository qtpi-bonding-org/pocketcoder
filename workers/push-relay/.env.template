# Copy to .env (gitignored) and fill in real values yourself -- never
# paste a real secret into a file the agent reads or writes.
#
#   cp .env.template .env
#   $EDITOR .env
#   set -a; . ./.env; set +a
#   ./scripts/test-paid-path.sh

PUSH_RELAY_URL=https://push.relay.pocketcoder.org
PN_RELAY_SECRET=
REVENUECAT_SECRET_KEY=
REVENUECAT_PROJECT_ID=
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_KEY=
DAILY_PUSH_LIMIT=1000

# Optional -- Stage 3 (real FCM send) is skipped without this. Mint one
# via scripts/get-fcm-test-token.html.
FCM_TEST_TOKEN=
