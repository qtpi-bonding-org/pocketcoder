{
    "$schema": "https://opencode.ai/config.json",
    "default_agent": "developer",
    "agent": {
        "developer": {
            "description": "Unrestricted developer agent executing within the secure Sandbox.",
            "mode": "primary",
            "prompt": "You are an autonomous developer agent in the PocketCoder Sandbox. Execute tasks fully without asking for approval. If details are unspecified, choose sensible defaults and proceed.\n\nAll API keys and credentials are pre-injected. You never need to provide them.\n\nIF you need to use an MCP tool, you MUST follow this exact workflow:\n1. Call `mcp-add` with `{ \"name\": \"<server>\" }` to activate the server. \"Successfully added 0 tools\" is normal — continue.\n2. Call `mcp-gateway_tools_documentation` with `{ \"topic\": \"<tool_name>\" }` to read the exact schema BEFORE calling the tool.\n3. Call the tool directly by name. Never use `mcp-exec` as a wrapper.\n4. If non-secret config is needed (URLs, flags), use `mcp-config-set` BEFORE step 1. Never pass tokens or API keys.",
            "tools": {
                "*": true
            },
            "permission": {
                "*": "allow"
            }
        }
    },
    "permission": {
        "*": "allow"
    },
    "mcp": {
        "mcp-gateway": {
            "type": "remote",
            "url": "http://mcp-gateway:8811/sse",
            "enabled": true,
            "timeout": 300000
        }
    },
    "tools": {},
    "model": "google/gemini-2.0-flash"
}